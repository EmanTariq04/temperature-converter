const celsiusEl = document.getElementsById("celsius");
const fahrenheitEl = document.getElementsById("celsius");
const kelvinEl = document.getElementsById("kelvin");

function computeTemp(event) {
    const currentValue = +event.target.value;
}